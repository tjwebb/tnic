$ = { };    // global

/**
 * @module com.tnic.sys.bootstrap
 * 
 * Bootstrap a new TNIC program by creating the environment in which it will
 * live. This code is included in the initial invocation of supervisor() and
 * init().
 */
(function () {
    print("hello I exist");
})();
